<UserControl x:Class="Gar.Desktop.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:uid="http://gar.com/ClientDesign"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=uid:MainViewModel, IsDesignTimeCreatable=True}">
    <DockPanel>
        <ContentControl DockPanel.Dock="Top" DataContext="{Binding MainMenuViewModel}" Content="{Binding}"/>
        <TabControl>
            <TabItem DataContext="{Binding FormattingToolsViewModel}" 
                     Header="{Binding ViewTitle}" 
                     Content="{Binding}"/>
        </TabControl>
    </DockPanel>
</UserControl>
